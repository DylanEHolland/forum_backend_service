create table posts (
    id serial,
    message text,
    primary key(id)
);

create table users (
    id serial,
    first_name text,
    last_name text,
    user_name text,
    pass_word text
);

create table posts (
    id serial,
    message text,
    primary key(id)
);